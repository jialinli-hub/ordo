# 2026-04-22 开发进度记录

## 今日完成

- 完成 `Ordo V1` 计划中的核心任务落地（Task 1 - Task 8），并将计划文档中的任务与步骤更新为已完成状态。
- 完成数据库初始化与迁移修复：
  - 重建基线迁移文件并成功执行迁移。
  - 本地 PostgreSQL `ordo` 数据库与 Prisma schema 已同步。
  - 已确认核心表创建成功：`Organization`、`OrganizationMember`、`Project`、`ProjectMember`、`ProjectIssueCounter`、`Cycle`、`Issue`、`IssueComment`。
- 后端能力已覆盖并可用：
  - `profile`、`workspace`、`team`、`cycle`、`issue`。
  - 鉴权改为纯 `idToken` 模式（不使用 session）。
- 登录能力完成升级：
  - 已接入真实 Google `id_token` 验签（`google-auth-library`）。
  - 前端接入 Google Identity Services 按钮。
  - 保留 `dev-google:<email>` 作为本地开发兜底登录方式。

## 今日验证结果

- `npm run lint` 通过。
- `npm run test`（API）通过，11/11。
- `npm run test -w apps/web` 通过。
- `npm run build` 通过。
- `GET /api/health` 可访问并返回正常状态。

## 当前状态

- 项目已具备可继续迭代的前后端基线。
- 数据库已就绪，迁移链路可用。
- 登录链路支持开发模式与真实 Google 验签模式。

## 明日继续建议

1. 配置真实 Google Client ID（前后端环境变量）并完成端到端登录联调。
2. 将内存存储（`memoryStore`）逐步替换为 Prisma 持久化实现（优先 `user/workspace/team`）。
3. 补齐关键 API 的契约测试与异常分支测试（401/403/404/非法参数）。
4. 增加初始化种子数据脚本，方便本地演示与联调。
