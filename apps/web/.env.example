# 钉钉开放平台应用 Client Id（可为空仅用本地跳过二维码）
VITE_DINGTALK_CLIENT_ID=

# —— API 基准地址 ——
# 【推荐本地】不要设置 VITE_API_BASE_URL（或保持未定义），这样会请求浏览器当前源的 /api/...，
# 由 apps/web vite.config.js 代理到后端，避免 SPA 误判为 HTML。
#
# 【直连后端】仅当必须用绝对地址访问 API 时再取消注释并改写：
# VITE_API_BASE_URL=http://127.0.0.1:3000
#
# 后端监听端口若非 3000，改下面一项（仅影响「未设置」VITE_API_BASE_URL 时的 Vite 代理）：
VITE_DEV_PROXY_API=http://127.0.0.1:3000
